# Changelog

## 0.1.0

### Core Features
- Agent configuration, publishing, templates, and cloning
- Agent custom workspace management
- AI generation interface (using default LLM)
- Agent batch field retrieval interface
- Strategy classification and strategy list APIs

### Configuration
- Tool timeout configuration
- Task planning mode toggle
- Knowledge entry quantity validation
- Agent self_config field structure query
- Data source configuration (metrics, knowledge entries)

### Permissions & Business Domains
- Business domain association
- Agent permission verification (app/business accounts)
- Agent tri-state status filtering

### Operations
- HTTP server graceful shutdown
- HTTP request logging (client/server separation)
- MQ configuration and audit logging
- SQL scripts embedded in microservices
- Redis/MQ dependency declarations

